# The v2 SDK (@opencode-ai/plugin) declares peerOptional @opentui/solid >=0.5.2,
# while this plugin's v1 TUI entry uses the v1 @opentui 0.4.x stack. We don't
# use the v2 SDK's TUI API, so accept the mismatch instead of forcing a bump
# that could break the v1 TUI panel.
legacy-peer-deps=true
