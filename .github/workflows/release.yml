name: Release

# Publishes @gitawego/opencode-vision to npm on tag push (v*).
# Uses the repository/org NPM_TOKEN secret (granular npm token, Read-and-write,
# scoped to @gitawego, with "Bypass 2FA" — no OTP needed). Set it on this repo
# (or its org):  gh secret set NPM_TOKEN
#
# Idempotent: skips if the version is already on npm (safe to re-run).
# Gates on typecheck + tests before publishing.

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write  # needed to create GitHub Releases on tag push

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
          registry-url: https://registry.npmjs.org

      - run: npm ci

      - run: npm run typecheck

      - run: npm test

      - name: Skip if already published
        id: check
        run: |
          set -u
          ver=$(node -p "require('./package.json').version")
          pkg=$(node -p "require('./package.json').name")
          existing=$(npm view "$pkg@$ver" version 2>/dev/null || true)
          if [ "$existing" = "$ver" ]; then
            echo "::notice::$pkg@$ver already on npm — skipping publish"
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "Publishing $pkg@$ver"
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish
        if: steps.check.outputs.skip != 'true'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public

      # Keep GitHub Releases in sync with npm: create a release for the tag
      # (auto-generated notes). Idempotent — skips if the release already exists.
      # Uses the auto-provided github.token (no third-party action).
      - name: Create GitHub Release
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="${GITHUB_REF_NAME}"
          if gh release view "$tag" >/dev/null 2>&1; then
            echo "::notice::Release $tag already exists — skipping"
          else
            gh release create "$tag" --generate-notes --title "$tag"
          fi
